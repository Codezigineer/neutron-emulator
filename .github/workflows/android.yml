name: Android CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: set up JDK 11
      uses: actions/setup-java@main
      with:
        java-version: '11'
        distribution: 'temurin'
        cache: gradle
    - name: Setup Node.js environment
      uses: actions/setup-node@main
      with:
        node-version: latest
    - name: Build with Gradle
      run: rm -rf .git/hooks;mkdir dist;npm install;npm run build;npx cap sync;cd android;chmod +x gradlew;./gradlew build
